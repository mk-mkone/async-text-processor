version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    networks:
      - backend

  mongodb:
    image: mongo:6
    container_name: mongo
    ports:
      - "27017:27017"
    networks:
      - backend

  worker:
    build: ./worker
    container_name: text_worker
    environment:
      AMQP_URL: amqp://guest:guest@rabbitmq/
      MONGO_URI: mongodb://mongo:27017
    depends_on:
      - rabbitmq
      - mongodb
    networks:
      - backend

networks:
  backend:
    driver: bridge
